@model SFA.DAS.FAA.Web.Models.AccountStubViewModel

<div class="govuk-grid-row" id="estimate-start-transfer">
    <div class="govuk-grid-column-two-thirds">
        <h1 class="govuk-heading-xl">You've signed in</h1>
        <p>Id: @Model.Id</p>
        <p>Id: @Model.Email</p>

        <table class="govuk-table">
            <thead class="govuk-table__head">
                <tr class="govuk-table__row">
                    <th scope="col" class="govuk-table__header">Account ID</th>
                    <th scope="col" class="govuk-table__header">Role</th>
                    <th scope="col" class="govuk-table__header">Candidate name</th>
                </tr>
            </thead>
@*             <tbody class="govuk-table__body">
                @foreach(var candidate in Model.Accounts)
                {
                    <tr class="govuk-table__row">
                        <td class="govuk-table__cell">@candidate.AccountId</td>
                        <td class="govuk-table__cell">@candidate.Role</td>
                        <td class="govuk-table__cell">@candidate.Name</td>
                    </tr>
                }
            </tbody> *@
        </table>

        <p>
            <a href="@Model.ReturnUrl" class="govuk-button">Continue</a>
        </p>

    </div>
</div>